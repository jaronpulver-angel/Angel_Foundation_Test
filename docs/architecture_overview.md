# Architecture Overview

## System Architecture

This document explains the complete flow of design tokens from Figma to all 8 supported platforms.

---

## High-Level Pipeline

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           DESIGN TOKEN PIPELINE                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│              │     │              │     │              │     │              │
│    FIGMA     │────▶│   EXPORT     │────▶│   GITHUB     │────▶│    STYLE     │
│   (Source)   │     │   (Manual)   │     │   (Storage)  │     │  DICTIONARY  │
│              │     │              │     │              │     │  (Transform) │
└──────────────┘     └──────────────┘     └──────────────┘     └──────┬───────┘
                                                                      │
                           ┌──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         PLATFORM-SPECIFIC OUTPUTS                            │
├─────────────┬─────────────┬─────────────┬─────────────┬─────────────────────┤
│ React Native│  React Web  │    Roku     │    tvOS     │  Android TV/Xbox/   │
│    (.ts)    │   (.css)    │   (.brs)    │  (.swift)   │  WebTV (.xml/.css)  │
└─────────────┴─────────────┴─────────────┴─────────────┴─────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            CI/CD AUTOMATION                                  │
│         GitHub Actions → Build → Test → Deploy to Staging → Production      │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Figma Token Structure

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              FIGMA VARIABLES                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 color_base/tokens.json                                                  │
│     ├── color.neutral (50-1000)    #fcfcfc → #141414                       │
│     ├── color.white (50-1000)      with alpha transparency                 │
│     ├── color.black (50-1000)      with alpha transparency                 │
│     ├── color.guild (50-1000)      brand orange                            │
│     ├── color.accent (50-1000)     teal (primary action)                   │
│     ├── color.danger (50-1000)     error red                               │
│     ├── color.warning (50-1000)    warning orange                          │
│     ├── color.success (50-1000)    success green                           │
│     └── color.information (50-1000) info cyan                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 color_theme/                                                            │
│     ├── light.json                                                          │
│     │   ├── surface (default, overlay, raised)                             │
│     │   ├── text (primary, secondary, tertiary)                            │
│     │   ├── divider (normal, lighter, darker)                              │
│     │   └── component.button.emphasis.[variant].[state]                    │
│     └── dark.json                                                           │
│         └── (same structure, different values)                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 dimensions/variables.json                                               │
│     ├── spacing (none, 4xs-27xl)   0 → 384                                 │
│     ├── border_radius             rounded_none → rounded_full               │
│     └── viewports                 phone_sm → desktop_lg                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 component/variables.json                                                │
│     └── button.size.[xs/sm/md/lg/xl]                                       │
│         ├── height, padding_horizontal, padding_vertical                   │
│         ├── border_radius, border_focus_radius                             │
│         ├── font_size, font_line_height, font_letter_spacing               │
│         └── icon_height, icon_font_size                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 typography/                                                             │
│     ├── mobile.json                                                         │
│     ├── tablet.json                                                         │
│     ├── desktop.json                                                        │
│     └── tv.json                  (1.25x larger for 10-foot UI)             │
│         └── font.family, font.weight, font.size, font.line_height          │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Export Process (Manual)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           EXPORT WORKFLOW                                    │
└─────────────────────────────────────────────────────────────────────────────┘

     Figma                    Plugin                     GitHub
       │                        │                          │
       │  1. Open Variables     │                          │
       │  2. Run Export Plugin  │                          │
       │─────────────────────▶ │                          │
       │                        │                          │
       │                        │  3. Generate JSON        │
       │                        │     (Tokens Studio       │
       │                        │      format)             │
       │                        │                          │
       │                 4. Download ZIP                   │
       │◀────────────────────── │                          │
       │                        │                          │
       │                              5. Upload to         │
       │                                 tokens/ folder    │
       │──────────────────────────────────────────────────▶│
       │                                                   │
       │                              6. Create PR         │
       │                                                   │
```

**Plugin:** "Variables Export" by Kel Browner (free)

---

## Repository Structure

```
angel-design-tokens/
├── tokens/                          ← Figma exports go here
│   ├── $metadata.json
│   ├── $themes.json
│   ├── color_base/
│   │   └── tokens.json
│   ├── color_base_brands/           (future: page theming)
│   ├── color_theme/
│   │   ├── light.json
│   │   └── dark.json
│   ├── dimensions/
│   │   └── variables.json
│   ├── component/
│   │   └── variables.json
│   ├── typography/
│   │   ├── mobile.json
│   │   ├── tablet.json
│   │   ├── desktop.json
│   │   └── tv.json
│   └── strings/                     (future: reusable strings)
│
├── fonts/                           ← Font source files
│   ├── WhitneySSm-Medium.ttf
│   ├── WhitneySSm-Semibold.ttf
│   ├── WhitneySSm-Bold.ttf
│   └── web/
│       ├── WhitneySSm-Medium.woff2
│       └── WhitneySSm-Bold.woff2
│
├── packages/                        ← Generated outputs
│   ├── react-native/
│   │   ├── src/tokens.ts
│   │   └── package.json
│   ├── web/
│   │   ├── tokens.css
│   │   ├── tokens.scss
│   │   └── package.json
│   ├── roku/
│   │   └── AngelTokens.brs
│   ├── tvos/
│   │   └── AngelTokens.swift
│   ├── android/
│   │   ├── colors.xml
│   │   └── dimens.xml
│   ├── xbox/
│   │   └── Tokens.xaml
│   └── web-tv/
│       ├── tokens.css
│       └── package.json
│
├── docs/                            ← Documentation
│   ├── architecture_overview.md
│   ├── token_structure_and_naming.md
│   ├── consumption_guide.md
│   ├── transformation_logic.md
│   ├── ci_cd_workflow.md
│   ├── versioning_and_releases.md
│   └── vision_and_scope.md
│
└── .github/workflows/               ← CI/CD
    └── build-tokens.yml
```

---

## Style Dictionary Transformation

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        STYLE DICTIONARY FLOW                                 │
└─────────────────────────────────────────────────────────────────────────────┘

          tokens/*.json
               │
               ▼
     ┌─────────────────┐
     │ Style Dictionary │
     │    Transform     │
     └────────┬────────┘
              │
     ┌────────┴────────────────────────────────────────────┐
     │                                                      │
     ▼                    ▼                    ▼            ▼
┌─────────┐        ┌─────────┐        ┌─────────┐    ┌─────────┐
│React    │        │Web      │        │Roku     │    │tvOS     │
│Native   │        │         │        │         │    │         │
│         │        │         │        │         │    │         │
│tokens.ts│        │tokens.  │        │Angel    │    │Angel    │
│         │        │css/scss │        │Tokens   │    │Tokens   │
│         │        │         │        │.brs     │    │.swift   │
└─────────┘        └─────────┘        └─────────┘    └─────────┘
```

### Transformation Example

**Input (Figma/Tokens Studio format):**
```json
{
  "color": {
    "accent": {
      "600": {
        "value": "#16b087",
        "type": "color"
      }
    }
  }
}
```

**Outputs:**

| Platform | Output |
|----------|--------|
| React Native | `colorAccent600: '#16b087'` |
| Web CSS | `--color-accent-600: #16b087;` |
| Roku | `ColorAccent600: "#16b087"` |
| Swift | `static let accent600 = UIColor(hex: "#16b087")` |
| Android | `<color name="color_accent_600">#16b087</color>` |
| XAML | `<Color x:Key="ColorAccent600">#FF16b087</Color>` |

---

## CI/CD Pipeline

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          GITHUB ACTIONS FLOW                                 │
└─────────────────────────────────────────────────────────────────────────────┘

  PR Merged to Main
         │
         ▼
┌─────────────────┐
│  Validate JSON  │ ← Schema validation, naming conventions
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Style Dictionary│ ← Transform to all platforms
│     Build       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Run Tests      │ ← Verify outputs
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Publish to npm  │ ← @angel/tokens-* packages
│ / Maven / SPM   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Trigger App     │ ← Auto-rebuild all apps
│ Deploy Workflows│    Deploy to staging
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Slack Notify    │ ← Alert #design-system channel
└─────────────────┘
```

---

## Data Flow Summary

```
Designer updates     →  Export with    →  Upload JSON   →  PR triggers
Figma Variables         free plugin       to GitHub        CI/CD
                                                              │
                                                              ▼
                                                   Style Dictionary
                                                   transforms tokens
                                                              │
                    ┌─────────────────────────────────────────┼─────────────────────────────────────────┐
                    │                                         │                                         │
                    ▼                                         ▼                                         ▼
            ┌───────────────┐                         ┌───────────────┐                         ┌───────────────┐
            │ Mobile/Web    │                         │ TV Platforms  │                         │ Xbox/Desktop  │
            │               │                         │               │                         │               │
            │ React Native  │                         │ Roku          │                         │ Xbox (XAML)   │
            │ React Web     │                         │ tvOS          │                         │               │
            │               │                         │ Android TV    │                         │               │
            │               │                         │ Vizio/Xumo    │                         │               │
            └───────────────┘                         └───────────────┘                         └───────────────┘
```

---

## Related Documents

- [Token Structure and Naming](./token_structure_and_naming.md) - Naming conventions
- [Transformation Logic](./transformation_logic.md) - Platform transformations
- [CI/CD Workflow](./ci_cd_workflow.md) - Automation details
- [Consumption Guide](./consumption_guide.md) - Platform usage
